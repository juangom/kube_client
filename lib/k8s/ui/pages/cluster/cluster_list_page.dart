
import 'package:flutter/material.dart';

class ClusterListPage extends StatelessWidget {
  const ClusterListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}